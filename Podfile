project 'AppSafe.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AppSafe' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppSafe
  pod 'PopupDialog'
  pod 'Alamofire' 
  pod 'RxSwift'
  pod 'RxCocoa'
  pod 'AlamofireObjectMapper'
  pod 'ObjectMapper'
  
  target 'AppSafeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AppSafeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
